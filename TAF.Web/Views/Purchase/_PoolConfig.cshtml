<div class="row">
<div class="col-xs-12 col-md-12">
<div class="widget">
<div class="widget-header bordered-bottom bordered-primary">
    <span class="widget-caption">抽取范围配置</span>
</div>
<div class="widget-body">
<div class="row">
<div class="col-lg-12 col-sm-12 col-xs-12">
<div class="tabbable">
<ul class="nav nav-tabs" id="myTab">
    <li class="tab-red active">
        <a data-toggle="tab" href="#designUnit" aria-expanded="false" id="pdesignUnit">
            设计单位
        </a>
    </li>
    <li class="">
        <a data-toggle="tab" href="#costUnit" aria-expanded="false" id="pcostUnit">
            造价单位
        </a>
    </li>
    <li class="">
        <a data-toggle="tab" href="#constructionControlUnit" aria-expanded="false" id="pconstructionControlUnit">
            监理单位
        </a>
    </li>
    <li class="">
        <a data-toggle="tab" href="#biddingAgency" aria-expanded="false" id="pbiddingAgency">
            招标代理单位
        </a>
    </li>
    <li class="">
        <a data-toggle="tab" href="#partyA" aria-expanded="false" id="ppartyA">
            甲方代表
        </a>
    </li>
    <li class="">
        <a data-toggle="tab" href="#expert" aria-expanded="false" id="pexpert">
            评标专家
        </a>
    </li>
</ul>
<div class="tab-content">
<div id="designUnit" class="tab-pane active">
    <div class="table-toolbar">
        <a href="javascript:void(0);" class="btn btn-primary " v-on:click="save()"><i class="fa    fa-floppy-o"></i> 保存</a>
    </div>
    <div class="row">
        <div class="col-md-5">
            <table class="table table-striped table-hover table-bordered ">
                <thead>
                <tr role="row">
                    <th style="width: 60px;" >
                        操作
                    </th>
                    <th style="width: 200px;">
                        名称
                    </th>
                </tr>
                </thead>
                <tbody>
                <tr v-for="item in list">
                    <td>
                        <div class="checkbox">
                            <label for="selectItem_{{item.itemId}}">
                                <input type="checkbox" v-model="selectItem" value="{{item.itemId}}" name="selectItem" id="selectItem_{{item.itemId}}">
                                <span class=" text"></span>
                            </label>
                        </div>
                    </td>
                    <td>{{item.name}}</td>
                </tr>
                </tbody>
                
            </table>
        </div>
    </div>
</div>
<div id="partyA" class="tab-pane ">
    <div class="table-toolbar">
        <a href="javascript:void(0);" class="btn btn-primary " v-on:click="save()"><i class="fa    fa-floppy-o"></i> 保存</a>
    </div>
    <div class="row">
        <div class="col-md-5">
            <table class="table table-striped table-hover table-bordered ">
                <thead>
                <tr role="row">
                    <th style="width: 60px;" >
                        操作
                    </th>
                    <th style="width: 200px;">
                        名称
                    </th>
                </tr>
                </thead>
                <tbody>
                <tr v-for="item in list">
                    <td>
                        <div class="checkbox">
                            <label for="selectItem_{{item.itemId}}">
                                <input type="checkbox" v-model="selectItem" value="{{item.itemId}}" name="selectItem" id="selectItem_{{item.itemId}}">
                                <span class=" text"></span>
                            </label>
                        </div>
                    </td>
                    <td>{{item.name}}</td>
                </tr>
                </tbody>
                
            </table>
        </div>
    </div>
</div>
<div id="costUnit" class="tab-pane ">
    <div class="table-toolbar">
        <a href="javascript:void(0);" class="btn btn-primary " v-on:click="save()"><i class="fa    fa-floppy-o"></i> 保存</a>
    </div>
    <div class="row">
        <div class="col-md-5">
            <table class="table table-striped table-hover table-bordered ">
                <thead>
                <tr role="row">
                    <th style="width: 60px;" >
                        操作
                    </th>
                    <th style="width: 200px;">
                        名称
                    </th>
                </tr>
                </thead>
                <tbody>
                <tr v-for="item in list">
                    <td>
                        <div class="checkbox">
                            <label for="selectItem_{{item.itemId}}">
                                <input type="checkbox" v-model="selectItem" value="{{item.itemId}}" name="selectItem" id="selectItem_{{item.itemId}}">
                                <span class=" text"></span>
                            </label>
                        </div>
                    </td>
                    <td>{{item.name}}</td>
                </tr>
                </tbody>
                
            </table>
        </div>
    </div>
</div>
<div id="constructionControlUnit" class="tab-pane ">
    <div class="table-toolbar">
        <a href="javascript:void(0);" class="btn btn-primary " v-on:click="save()"><i class="fa    fa-floppy-o"></i> 保存</a>
    </div>
    <div class="row">
        <div class="col-md-5">
            <table class="table table-striped table-hover table-bordered ">
                <thead>
                <tr role="row">
                    <th style="width: 60px;" >
                        操作
                    </th>
                    <th style="width: 200px;">
                        名称
                    </th>
                </tr>
                </thead>
                <tbody>
                <tr v-for="item in list">
                    <td>
                        <div class="checkbox">
                            <label for="selectItem_{{item.itemId}}">
                                <input type="checkbox" v-model="selectItem" value="{{item.itemId}}" name="selectItem" id="selectItem_{{item.itemId}}">
                                <span class=" text"></span>
                            </label>
                        </div>
                    </td>
                    <td>{{item.name}}</td>
                </tr>
                </tbody>
                
            </table>
        </div>
    </div>
</div>
<div id="biddingAgency" class="tab-pane ">
    <div class="table-toolbar">
        <a href="javascript:void(0);" class="btn btn-primary " v-on:click="save()"><i class="fa    fa-floppy-o"></i> 保存</a>
    </div>
    <div class="row">
        <div class="col-md-5">
            <table class="table table-striped table-hover table-bordered ">
                <thead>
                <tr role="row">
                    <th style="width: 60px;" >
                        操作
                    </th>
                    <th style="width: 200px;">
                        名称
                    </th>
                </tr>
                </thead>
                <tbody>
                <tr v-for="item in list">
                    <td>
                        <div class="checkbox">
                            <label for="selectItem_{{item.itemId}}">
                                <input type="checkbox" v-model="selectItem" value="{{item.itemId}}" name="selectItem" id="selectItem_{{item.itemId}}">
                                <span class=" text"></span>
                            </label>
                        </div>
                    </td>
                    <td>{{item.name}}</td>
                </tr>
                </tbody>
                
            </table>
        </div>
    </div>
</div>
<div id="expert" class="tab-pane ">
    <div class="table-toolbar">
        <a href="javascript:void(0);" class="btn btn-primary " v-on:click="save()"><i class="fa    fa-floppy-o"></i> 保存</a>
    </div>
    <div class="row">
        <div class="col-md-5">
            <table class="table table-striped table-hover table-bordered ">
                <thead>
                <tr role="row">
                    <th style="width: 60px;" >
                        操作
                    </th>
                    <th style="width: 200px;">
                        名称
                    </th>
                </tr>
                </thead>
                <tbody>
                <tr v-for="item in list">
                    <td>
                        <div class="checkbox">
                            <label for="selectItem_{{item.itemId}}">
                                <input type="checkbox" v-model="selectItem" value="{{item.itemId}}" name="selectItem" id="selectItem_{{item.itemId}}">
                                <span class=" text"></span>
                            </label>
                        </div>
                    </td>
                    <td>{{item.name}}</td>
                </tr>
                </tbody>
                
            </table>
        </div>
    </div>
</div>
</div>
</div>
<div class="horizontal-space"></div>
</div>

</div>
</div>
</div>
</div>
</div>

<script src="~/Views/Purchase/poolConfig.js"></script>